<?php
	
	// define plugin version option key
	if ( !defined( 'PG_LT_VERSION_KEY' ) ) {
        define( 'PG_LT_VERSION_KEY', 'pg_live_tweets_version' );
    }

    // define plugin version option value
    if ( !defined( 'PG_LT_VERSION_VAL' ) ) {
        define( 'PG_LT_VERSION_VAL', '0.0.1' );
    }

	// define cache dir option key
    if ( !defined( 'PG_LT_CACHE_DIR_KEY' ) ) {
        define( 'PG_LT_CACHE_DIR_KEY', 'pg_live_tweets_cache_dir' );
    }

?>